@using NanolekPrototype.EntityModels.Enums
@model NanolekPrototype.EntityModels.Models.TableProductionPersonnel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TableProductionPersonnel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <input type="hidden" asp-for="PackagingProtocolId" value="@Model.PackagingProtocol.Id" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FullName" class="control-label"></label>
                @Html.DropDownList("FullNameId", ViewBag.FullNames, null, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label asp-for="Step" class="control-label"></label>
                <select asp-for="@Model.Step" asp-items="Html.GetEnumSelectList<ProductionPersonnelStep>()"></select>
            </div>
            <div class="form-group">
                <label asp-for="Role" class="control-label"></label>
                <select asp-for="@Model.Role" asp-items="Html.GetEnumSelectList<ProductionPersonnelRole>()"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="PackagingProtocols" asp-action="Details" asp-route-id="@Model.PackagingProtocol.Id">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
